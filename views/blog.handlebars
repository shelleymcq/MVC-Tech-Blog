<div class="text-left">
  <h2>{{name}}</h2>
  <h6>Created by {{user.name}} on {{format_date date_created}}</h6>
  <p>{{description}}</p>
  <p>
    <button class="btn btn-primary">Keep this Button?</button>
  </p>
</div>
